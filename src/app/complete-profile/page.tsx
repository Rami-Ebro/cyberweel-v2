"use client";import {FormEvent,useState}from"react";import {useRouter}from"next/navigation";
export default function CompleteProfile(){const router=useRouter();const[role,setRole]=useState<"PARTNER"|"AMBASSADOR">("PARTNER");const[msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/profile",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(b)});const d=await r.json();if(r.ok)router.replace(d.redirectTo);else setMsg("أكمل جميع الحقول المطلوبة") }return <main dir="rtl" className="min-h-screen bg-[#F4F1EA] p-6"><form onSubmit={submit} className="mx-auto max-w-2xl rounded-3xl bg-white p-8 shadow"><p className="font-bold text-[#9A7D43]">خطوة إلزامية قبل بدء العمل</p><h1 className="mt-2 text-3xl font-black">استكمال الملف</h1><div className="mt-5 flex gap-2"><button type="button" onClick={()=>setRole("PARTNER")} className="rounded-lg border p-2">شريك تنفيذ</button><button type="button" onClick={()=>setRole("AMBASSADOR")} className="rounded-lg border p-2">سفير</button></div><div className="mt-6 grid gap-4"><input required name="phone" placeholder="رقم التواصل" className="rounded-lg border p-3"/>{role==="PARTNER"?<><input required name="specialty" placeholder="التخصص" className="rounded-lg border p-3"/><textarea required name="experience" placeholder="الخبرة المهنية" className="rounded-lg border p-3"/><input required name="availability" placeholder="التوفر" className="rounded-lg border p-3"/><input name="portfolioUrl" placeholder="رابط الأعمال (اختياري)" className="rounded-lg border p-3"/></>:<><input required name="country" placeholder="البلد" className="rounded-lg border p-3"/><input required name="contactMethod" placeholder="طريقة التواصل المفضلة" className="rounded-lg border p-3"/><input required name="payoutMethod" placeholder="طريقة استلام العمولات" className="rounded-lg border p-3"/><textarea required name="payoutDetails" placeholder="بيانات استلام العمولات" className="rounded-lg border p-3"/></>}<button className="rounded-xl bg-[#111827] p-3 font-bold text-white">حفظ وبدء العمل</button>{msg&&<p className="text-red-700">{msg}</p>}</div></form></main>}
